<view class="list">
  <view url="/pages/detail/index?skill={{item.skill}}" class="hero" wx:for="{{heroList}}"
  wx:key="heroName">
    <image src="{{item.icon}}"></image>
    <text>{{item.heroName}}</text>
  </view>
</view>